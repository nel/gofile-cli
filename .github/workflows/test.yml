name: Test
on:
  push:
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        run: shellcheck gofile tests/test_gofile.sh
      - name: Tests
        run: ./tests/test_gofile.sh
